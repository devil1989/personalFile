1.获取长度用方法size
2.判断是否存在用??
3.所有的方法调用，用?而不是.(不管是在${}中还是在<#if>等=条件语句中)
4.属性调用要用符号.
5.assign声明变量，变量之间用空格，结束的地方不需要分号来结尾
6.声明的变量，在其他地方也可以使用
7.<#if （条件） >:(千万别丢括号)
8.<@debug.printr />:(千万别丢最后的“/”符号)

9.所有接口获取数据，对于的result都不是正确的返回数据，正确的返回数据是其他名称，但是result里面的所有参数都是正确的。
10,<#list></#list>只能用于数组，不能用于对象