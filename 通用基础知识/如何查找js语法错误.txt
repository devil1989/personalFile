在浏览器中直接可以通过报错提示来看到底第几行出错，
错误提示的第一行或者第二行末尾会有出错的“文件名：出错的行数”，
接下来的几行红色的就是依次的函数调用顺序，A被B调用，B被C调用之类的


如果在app或者手机浏览器上要找到第几行出错，可以在html文件的末尾添加
window.onerror=function(a,b,c,d,e){
	alert(e.stack)//具体每个参数的含义可以查一下，e.stack中包含了对应出错的详细信息（包括第几行第几列，哪个js文件）
}
